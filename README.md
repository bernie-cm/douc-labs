Simple application to test GitHub Actions CI/CD pipeline automation tool.

The application will use GitHub Actions instead of the original setup of Jenkins.

- Very simple web app has been containerised. The next step will be to set up a simple Jenkins pipeline to automatically push the container image to Docker Hub.
- Set up a Jenkins multi branch pipeline and test it.

- Testing the new branch on GitHub and multi branch pipeline scanning.

Another test to assess how the GitHub actions workflow works.